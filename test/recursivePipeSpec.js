
var expect = require("chai").expect;
var recersivePipe = require("../lib/recersivePipe.js");

describe("recersivePipe takes uri and optional destination path and streams the data to ./ or desination", function(){

   describe("#recersivePipe()", function(){
       it("It Should get an array of URI and stream the data", function(){
           var urls = ["http://google.com"];
       });
       it("Should pop off an array value pass that to steam");
       it("If length of array is > 0 it should recursively call its self with the curreny arraay no out the value that was popped");
    });
});
