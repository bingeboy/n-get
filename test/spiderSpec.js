
var expect = require("chai").expect;
var uriManager = require("../lib/spider");

describe("Takes the path from the -rl flag and crawls", function(){

   describe("startCrawling", function(){
       it("should make a requset", function(){
       });
    });

   describe("log fetches to array", function(){
       it("log array should be writing to disk", function(){
       });
    });

   describe("requests for pipes will be taken from stream and data callbacks stored to disk", function(){
       it("", function(){
       });
    });


});
